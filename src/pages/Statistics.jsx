import React from 'react';
import useTitle from '../utility/useTitle';

const Statistics = () => {
    useTitle('|| Statistics')
    return (
        <div>
            
        </div>
    );
};

export default Statistics;